## Laravel MongoDB CRUD Application

This is a simple CRUD application built with Laravel using MongoDB as the database. The project demonstrates how to create, read, update, and delete records using Laravel with MongoDB integration.
